# Evaluation-only dependencies (your app deps are separate)
scikit-learn>=1.3
matplotlib>=3.7
numpy>=1.24
pytest>=7.4
# For RAG faithfulness/relevancy (optional but recommended):
ragas>=0.1.0
datasets>=2.14